收到。您提供了一套成熟的“微服务网关 + 统一鉴权”基础设施规范，这对于规范 `ai-scene-to-video` 项目的接入非常有帮助。

虽然 `ai-scene-to-video` 目前处于 MVP 阶段（单体 Spring Boot + Python Worker），但为了符合您的基建标准，我们需要将其视为一个标准的**业务应用**进行接入。

我将结合您现有的 `.trae/rules/core.md` 和您刚才提供的 **Trae 个人规则**，为您总结一份针对本项目的开发规则文档。这份规则将明确：
1.  **架构原则**: 不再裸奔，接入统一网关和鉴权。
2.  **网络与路由**: 后端服务如何加入共享网络，路由如何配置。
3.  **鉴权**: 业务代码不再处理 Auth，而是信任网关透传的 Header。
4.  **可观测性**: 必须实现 Request ID 透传和健康检查。
5.  **依赖**: 复用统一的 Supabase/Upstash，不自行维护中间件。

总结后的规则将保存为 `.trae/rules/ai-scene-to-video-rules.md` (或替换 core.md，视您意愿，建议新建专用规则文件以免覆盖通用规则)。

请确认是否执行？
